# public/

Static assets ที่เบราว์เซอร์โหลดตรงๆ

- `css/` — ไฟล์ CSS (เริ่มต้นที่ `main.css`)
- `js/` — ไฟล์ JavaScript ฝั่ง client (เริ่มต้นที่ `main.js`)
- `img/` — รูปภาพ
- `icons/` — icons และ favicon

URL prefix:
- `/css/*`
- `/js/*`
- `/img/*`
- `/icons/*`