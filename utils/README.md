# utils/

ฟังก์ชันช่วยเหลือทั่วไปที่ไม่ขึ้นกับ business logic

- `logger.js` — ระบบ log (error/warn/info/debug)
- `response.js` — ตัวช่วยส่ง response (success, error, redirectBack)
- `validation.js` — ตัวตรวจสอบข้อมูล (email, username, password)